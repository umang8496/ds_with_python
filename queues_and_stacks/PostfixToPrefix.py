# Algorithm :
# Scan from Left to Right and repeat the following steps
# TOKEN           OPERATION
# operand         add to stack
# operator        pop stack into s1
#                 pop stack into s2
#                 then concatenate: operator s2 s1
#                 and push the result into the stack